# mkz1 PXE Server Configuration
# Copy to pxe.env and customize for your environment
#
# Workflow:
#   1. cp pxe.env.example pxe.env && edit pxe.env
#   2. make prepare-iso    # Download and extract on host
#   3. make build run      # Start container
#
# For remote deployment:
#   rsync -av build/ remote:/pxe/

# Required: URL to download Linux ISO (used by 'make prepare-iso')
ISO_URL=https://releases.ubuntu.com/24.04/ubuntu-24.04-desktop-amd64.iso

# IP address of the PXE server (host IP on the network clients will boot from)
SERVER_IP=192.168.0.1

# DHCP range: start,end,lease_time
DHCP_RANGE=192.168.0.50,192.168.0.100,12h

# Network interface inside container
INTERFACE=eth0

# Container internal ports (standard ports - mapped from host 20xx via podman)
# Host nftables redirects: 67->2067, 69->2069, 80->2080
# Podman maps: 2067->67, 2069->69, 2080->80
DHCP_PORT=67
TFTP_PORT=69
HTTP_PORT=80
